import 'package:flutter/material.dart';

class Addgoods extends StatelessWidget {
  const Addgoods({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text("ldsmfksdfsfs")],
    );
  }
}
